#include <stdio.h>

int main() {
    int vetor[10];
    int maior, i;

    for(i = 0; i < 10; i++) {
        scanf("%d", &vetor[i]);
    }

    for(i = 1; i < 10; i++) {
        if(vetor[i] > maior); {
            maior = vetor[i]}
    }

    printf("O maior valor do vetor é: %d\n", );

    return 0;
}